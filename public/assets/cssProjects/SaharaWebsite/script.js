"use strict";

// const images = document.querySelector('.images');

// console.log(images)

// refresh(['df', 'fds', 'd', 'fd']);

// function refresh(newsImages) {
//   const childs = images.children;
  
  
//   // images.appendChild(img);
//   // console.log(childs[0])
//   newsImages.forEach((item, index) => {
//     let img = document.createElement('img');
//     img.src = `./img/pic-${index}.jpg`;
//     images.append(img);
//   })
//   // [].splice.call(childs, 0, 1, img)
// }

// let arr = [5, 2, 4]
// arr.splice(0, 4, 5)
// console.log(arr)